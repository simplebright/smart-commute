server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/smart_commute?useSSL=false&serverTimezone=UTC
    username: root
    password: gx6073329
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  data:
    redis:
        host: localhost
        port: 6379
        password: 
        database: 0
        timeout: 3000ms
        lettuce:
            pool:
                max-active: 20
                max-wait: -1ms
                max-idle: 8
                min-idle: 0

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.smartcommute.entity
  configuration:
    map-underscore-to-camel-case: true

jwt:
  secret: my-very-long-secret-key-that-is-at-least-32-characters-long
  expiration: 86400000  # 24小时

third-party:
  map:
    api-key: your-map-api-key
    base-url: https://maps-api.example.com
  payment:
    app-id: your-payment-app-id
    merchant-id: your-merchant-id
    base-url: https://payment-api.example.com

logging:
  level:
    com.smartcommute: debug